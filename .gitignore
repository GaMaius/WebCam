# dependencies
/node_modules

# next.js
/.next/
/out/
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo

# local Claude tooling (not part of the product)
.agents/
.claude/skills/

.vercel
.env*

# Raw third-party avatar downloads (4K PNG texture sets are ~600MB+ and must
# never be committed). Run `node scratch/build_avatar_assets.mjs` to produce the
# web-sized copy under public/models/spiderman/, which IS committed.
/public/models/spider-man-brand-new-day/
